server:
  port: 8083
spring:
  application:
    name: config
  #cloud:
    #config:
      #server:
        #git:
          #uri: https://github.com/29DCH/config-repo
          #username: 29DCH
          #password:
          #basedir: /home/zq/SpringCloudLearning/SpringCloudDemo/config/basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
#management:
  #endpoints:
    #web:
      #expose: "*"